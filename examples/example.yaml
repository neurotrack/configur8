top:
  label: Hello "this:George"
  environment: cli:STAGE
  secret: aws-secretsmanager:/(cli:STAGE)/auth0:FOO
  # clientId: aws-parameterstore:/(cli:ENVIRONMENT)/test/CLIENT
  arrayValues:
    - aws-secretsmanager:/(cli:STAGE)/auth0:HELLO
    - cli:STAGE
    - aws-secretsmanager:/(cli:STAGE)/auth0:NOT_THERE
  